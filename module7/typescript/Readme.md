for rebuild project run
<code>
    npm run build
</code>

open in browser index.html and switch to console <br/>
to switch between eu, us and eng apps make changes to <code>webpack.config.js</code> <br/>
and run rebuild
